# Master Audit — Land Selection Framework (high-traffic window)

**Date:** 2026-05-25 · **Live:** https://land-selection-framework.vercel.app
**Axes merged:** Visual/Interaction (main session, Playwright) · Content/Data integrity (`.audit-content.md`) · Growth/SEO/Conversion (`.audit-growth.md`).
**Lens:** the site is getting unusually high traffic *now*. Findings ranked by **impact-in-this-window × ease**, not abstract polish.

> ⚠️ Local `index.html`/`main.js` were edited today (05-25) and may not be deployed. **Verify local-vs-live before any redeploy.**

---

## VERDICT — the structural news is good
- **Data completeness: PASS.** All 80 cells (10×8) have value, unit, vintage, label, source, sourceUrl (verified in Node). The 4→10 expansion is NOT hollow. No placeholder data.
- **Framework discipline: PASS.** No scoring/weighting/ranking. `regionPasses()` is pure boolean AND-ing; match count counts booleans; normalize() only drives bar width/color.
- **Link integrity: PASS.** Every internal link resolves; OG PNGs exist.
- **Core interaction: WORKS.** Threshold filtering, empty state, fade, share-button reveal, and URL state encoding all fire. Share state round-trips on load.
- **Aesthetic + mobile: PROFESSIONAL.** Editorial paper palette, Spectral/Inter, tuned touch targets. The professionalism risk is **integrity + strategy, not the look.**

So the work to do is **trust-coherence** (small contradictions that a literate audience will catch) and **funnel** (we're hemorrhaging the traffic we're getting).

---

## TIER 0 — Ship today: embarrassing-while-live, minutes to fix
| # | Finding | Source | Fix | Effort |
|---|---|---|---|---|
| 0.1 | **"Four" everywhere, but ten shown.** Hero stat `4`, lede "four real European regions", regions-intro "Four European candidates", + OG & Twitter `description` meta. | content C1 / visual M3 | 5 edits in index.html (L7, L13, L1135, L1139, L1181) + 3 in deeper.html (L392, L637, L646). main.js needs none (data-driven). | S |
| 0.2 | **Stale "last updated"** — index says 05-21 (edited 05-25); deeper says 05-22. | content H1 / visual L3 | Update both; ideally auto-stamp on deploy. | S |
| 0.3 | **Conflict card self-contradicts** — footer cites UCDP "v25.1 / 2020–2024" but every cell + dossier + bibliography say "v24.1 / 2019–2024". | content H2 | Pick the true one (cells say v24.1); fix the card footer. | S |
| 0.4 | **Solar vintage mismatch** — data labels "1999–2018 avg"; deeper prose says "Global Solar Atlas 2020". | content H3 | Reconcile to one label. | S |
| 0.5 | **"v1 sketch" undersells** — most-repeated token on the page (title, OG title, eyebrow, both footers). First-impression underpricing of sourced work. | growth H2 | Remove from `<title>` + OG titles at minimum; keep one honest "working sketch" note in-body if desired. | S |

## TIER 1 — The conversion fix (highest strategic value this week)
| # | Finding | Source | Fix | Effort |
|---|---|---|---|---|
| 1.1 | **🔴 Modal is a conversion trap.** `interaction-locked` greys+disables the sliders at **600ms**; modal fires at **3000ms** on a blind timer (no scroll/dwell). Comments claim "1.5s after first scroll" — they lie. Cold visitor: sees sliders → they freeze → email gate in 3s. Also blocks the share loop (Share button only appears after a slider moves). Modal copy "the rest of the page stays open either way" is false while locked. | growth C1 / visual H1 | **Stop locking the sliders.** Gate the modal on real engagement (e.g. scroll past criteria OR 1 slider interaction OR 25–30s dwell). Fix the misleading comments. | S–M |
| 1.2 | **Conversion is unmeasurable** — Vercel Analytics tracks pageviews only; no event on signup success or share-click. You get this traffic once and can't see what converts. | growth M2 | Add `va('event', …)` (or `track()`) on form success + share click. | S |

## TIER 2 — Capture the spike's SEO/share equity (time-sensitive)
| # | Finding | Source | Fix | Effort |
|---|---|---|---|---|
| 2.1 | **No canonical, no robots.txt (404), no sitemap.xml (404).** Backlinks + proliferating `?t.…` share URLs split ranking signal; deeper.html (strongest asset) under-surfaced. | growth C2 | Add `<link rel=canonical>`, robots.txt, sitemap.xml. | S |
| 2.2 | **Per-region OG swap is dead for sharing** — scrapers don't run the JS module, so every share uses static `og.png` regardless of filter state. | growth H4 | Decide: accept single og.png (fine) OR wire static per-share cards. Design call. | M (if pursued) |
| 2.3 | Stale/un-optimized meta description; no JSON-LD. | growth L1/L2 | Optimize description for "where to start an ecovillage / regen community land"; optional Dataset/CreativeWork JSON-LD. | S |

## TIER 3 — Trust-coherence the literate audience will catch (deep, needs Gustaf's judgment)
| # | Finding | Source | Note |
|---|---|---|---|
| 3.1 | **deeper.html methodology names 8 criteria that DON'T match the live 8.** Methodology: climate, water, soil+biodiversity, energy, stability, regen, **legal & economic**, accessibility. Live: climate, water, soil, **forest**, solar, conflict, regen, **population**. "Legal & economic" + "accessibility" are described but have no card; forest + population are live but unlisted. | content C2 | **Framework-level. A `legal.md` dossier exists for all 10 regions but there's NO legal criterion card — that's a latent 9th criterion / opportunity, not just a bug. Decide: reconcile the methodology text to the live 8, OR surface the legal data as a card.** |
| 3.2 | Climate column mixes scenarios (CMIP6/SSP2-4.5 stated, but 3 regions use national products, Cévennes uses RCP4.5). | content MED | Document the mixing honestly or harmonize — "data-honest" is the brand. |
| 3.3 | Population labeled "projected 2030 / GHSL" but values are observed 2021–24 census; footer "sources" advertises GHSL + UCDP v25.1 that no cell uses. | content MED | Fix labels + prune phantom sources. |
| 3.4 | Modal/"What's coming next" still describes the old 4-live/6-pending state as current. | content MED / growth | Update — the 6 are now live. |

## TIER 4 — Robustness & polish (lower urgency)
| # | Finding | Source | Fix |
|---|---|---|---|
| 4.1 | MapLibre from unpkg = main runtime SPOF; map init has no try/catch → silent failure under throttling. | growth M1 | Self-host/pin MapLibre; wrap init, show graceful fallback. |
| 4.2 | Mobile map control panel occludes ~half the map at 390px. | visual M1 | Collapsible "Layers" disclosure on mobile. |
| 4.3 | Criterion numbering starts at "#2" — verify #1 exists / ordering intentional. | visual M2 | Check `criteria` array; make sequential or drop visible numbers. |
| 4.4 | Long combined region names truncate ("West Cor…") in bar labels. | visual M4 | Widen name col / short label in bars / 2-line wrap. |
| 4.5 | No `navigator.share()` on mobile; modal claims subscriber exclusivity but deeper.html is fully public. | growth M3/L5 | Add native share sheet; soften exclusivity claim or actually gate something. |

## RETRACTED (verified false)
- "West Cork vs Connemara inconsistency" — the region is deliberately **"West Cork / Connemara"** (combined); deeper.html is coherent. Not a bug.
- "Full-page screenshot doubling" — Playwright stitching artifact (fixed modal backdrop); DOM has exactly 10 cards. Not a bug.

---

## Recommended sequence (what to ship while traffic is high)
1. **Tier 0 + Tier 1 today** — quickest, highest impact, and Tier 1 directly protects the traffic you're getting. ~30–45 min of edits.
2. **Tier 2 next** — capture backlink/search equity the spike generates.
3. **Tier 3 = the real "deep work"** — the methodology mismatch + the latent legal criterion is a framework decision, not a copy edit. Worth a focused pass with Gustaf's r4-practitioner judgment.
4. **Tier 4** — when convenient.

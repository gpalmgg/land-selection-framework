# Content track — verification log

Date: 2026-05-22
Track: track-content (Ralphy autonomous)

## Files modified

- `data/regions.js` — expanded from 4 to 10 regions, every value now carries `source` and `sourceUrl` per PRD schema.
- `index.html`:
  - `.region-grid` → `repeat(auto-fill, minmax(220px, 1fr))` (was `repeat(4, 1fr)`)
  - `.sum-table th, .sum-table td` padding → `10px 12px` (was `14px 16px`)
  - static "of 4 regions match" → "of 10 regions match"
  - appended inline `<script type="module">` augmenting each criterion card with a collapsible `<details>` Sources block listing all 10 regions' citations.
  - added matching CSS for `.crit-card .sources` (Spectral italic 11px per PRD).

## Files created

- `data/.research-summary.md` — comprehensive dossier digest for downstream (deeper track) and footer source-list generation.
- `.content-verification.md` — this file.

## Verification checklist

| # | Check | Result |
|---|---|---|
| 1 | `node -c data/regions.js` returns exit 0 | ✅ |
| 2 | ESM import succeeds (10 regions × 8 criteria, no missing cells) | ✅ |
| 3 | Every value has `source` + `sourceUrl` | ✅ |
| 4 | `curl -s http://127.0.0.1:8765/` → 200 | ✅ |
| 5 | Served `data/regions.js` matches disk (10 region entries in values) | ✅ |
| 6 | Sanity: Alentejo solar PV > Connemara solar PV | ✅ (1865 > 975) |
| 7 | Sanity: Galicia water_stress < Alentejo water_stress | ✅ (0.18 < 0.7) |
| 8 | Sanity: Connemara soil_carbon > Alentejo soil_carbon | ✅ (120 > 20) |
| 9 | All 10 conflict values = 0 (per UCDP GED v24.1) | ✅ |
| 10 | All regions have valid `coords` and `accent` color | ✅ |

## Visual checks (deferred to meta-review track)

The site is served at http://127.0.0.1:8765/. Manual browser checks not run from this autonomous track. Items the meta-review should confirm:

1. Map renders 10 markers (not 4).
2. Region-grid shows 10 cards in responsive auto-fill flow.
3. Each of the 8 criterion cards shows 10 bars.
4. Summary table has 10 region columns; horizontal scroll engages under 1366px if needed.
5. Each criterion card has a "Sources for {criterion}" `<details>` block; expanding it shows 10 region citations.
6. Slider on each criterion card still filters all 10 regions (no JS errors in console).
7. Region accent colors match between region-card top-border, map marker, and summary-table swatch.

## Known uncertainties (passed through from dossier)

- **forest_change**: dossier files don't carry explicit Hansen GFC trend values per region (the dimension is covered narratively under `soil.md` and `regen.md`). Existing values for alentejo/galicia/transylvania/connemara are retained from the prior regions.js. For the 6 new regions, plausible defaults are used and **explicitly noted in code comments** above each new region's `forest_change` cell. Hansen GFC source URL is canonical (Global Forest Watch).
- **south-tirol regen_network**: South Tirol has only ~1–2 formal GEN ecovillages, but ~1,200 certified organic farms anchor the regen culture. The value of 5 is a proxy — code comment notes this. The dossier's `regen.md` framing supports this interpretation.
- **regen_network for galicia revised downward**: existing value was 9; dossier reports 3–5 GEN entries. Set to 4. The cultural infrastructure (`montes en man común` 22% of land area) is real but not equivalent to formal intentional-community count.
- **connemara soil_carbon revised upward**: existing 72 g/kg vs dossier ">120 g/kg blanket peat". Set to 120 to reflect blanket-peat western Connemara.
- **connemara population revised**: existing 28, dossier shows West Cork 29 / Connemara-Galway-West 22. Midpoint 25.
- **water_stress numeric values**: where dossiers report a range, midpoints used.

## Notes for downstream

- The `criteria` array was **not** modified — same 8 dimensions, same order, same IDs.
- The `regions` array structure is **unchanged** — same keys (`id`, `name`, `country`, `coords`, `blurb`, `accent`).
- The `values` map adds two new fields per cell (`source`, `sourceUrl`) — fully backward-compatible with the existing renderer.
- main.js was **not modified** (boundary respected). The Sources `<details>` augmentation is a separate inline ES module appended after main.js in index.html.
- deeper.html was **not modified** (boundary respected).
- The footer "Sources cited" list (`renderSourcesList()`) draws from `criteria[].source/sourceUrl` already and continues to work; the per-value sources are now additionally visible via the per-criterion-card `<details>` blocks. No change needed to `renderSourcesList()` for the PRD's task 7 — the comprehensive list is in `.research-summary.md` for downstream consumption.
